# Development extras — includes all runtime deps plus testing and notebooks.
# Minimum version bounds for direct dev dependencies; compatible with Python 3.10+.
#
# To install:
#   pip install -r requirements-dev.txt

-r requirements.txt

# Testing & lint
pytest>=8.0
pytest-cov>=5.0
ruff>=0.9
# HDBSCAN — matches pyproject [clustering-extras]; needed so tests/unit/test_density.py does not skip
hdbscan>=0.8

# Packaging (PyPI)
build>=1.0
twine>=5.0

# Notebooks
ipykernel>=6.0
jupyterlab>=4.0
