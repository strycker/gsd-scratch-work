---
phase: 37-v1-5-fork-dependency-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPENDENCIES.md
  - README.md
  - docs/CURSOR.md
autonomous: true
requirements:
  - TMPL-01
user_setup:
  - None (documentation-only).
must_haves:
  truths:
    - "`docs/DEPENDENCIES.md` exists and states `pyproject.toml` is canonical for package deps."
    - "`README.md` contains a pointer to `docs/DEPENDENCIES.md` under Installation (or immediately after)."
    - "`grep -q 'DEPENDENCIES.md' docs/CURSOR.md` exits 0."
  artifacts:
    - path: docs/DEPENDENCIES.md
      provides: Single source of truth narrative for forks
---

<objective>
Satisfy **TMPL-01**: document **fork onboarding** and **single source of truth** for dependencies (`pyproject.toml` vs `requirements*.txt`), with links from **README** and **CURSOR** docs.
</objective>

<execution_context>
@.planning/phases/37-v1-5-fork-dependency-docs/37-CONTEXT.md
@.planning/REQUIREMENTS.md
@pyproject.toml
@requirements.txt
@requirements-dev.txt
@scripts/setup.sh
</execution_context>

<tasks>

<task type="auto" tdd="false">
  <name>37-01-01 — Add docs/DEPENDENCIES.md</name>
  <read_first>
    - `pyproject.toml` (full file — `[project]`, optional deps, Python version)
    - `requirements.txt` (header + first 15 lines)
    - `requirements-dev.txt` (header + first 20 lines)
    - `scripts/setup.sh` (lines 1–120, install steps)
  </read_first>
  <action>
    Create **`docs/DEPENDENCIES.md`** with these sections (use real names from `pyproject.toml`):

    1. **Canonical definition** — The installable package **`trading-crab-lib`** and its dependencies are defined in **`pyproject.toml`** (`[project]` dependencies, `[project.optional-dependencies]` for `dev`, `data-extras`, `clustering-extras`). PyPI / `pip install trading-crab-lib` uses this file.

    2. **requirements.txt vs pyproject** — **`requirements.txt`** lists **minimum bounds** for a flat `pip install -r` install; its header already states alignment with **`pyproject.toml`**. It is a **convenience** for environments that do not use editable install; maintainers should keep bounds in sync when changing **`pyproject.toml`**.

    3. **requirements-dev.txt** — Dev + test + notebook tooling; aligns with **`pip install -e ".[dev]"`** (state the equivalence explicitly).

    4. **Scripts** — **`scripts/setup.sh`** installs from **`requirements.txt`** or **`requirements-dev.txt`** with `-r`; **`pip install -e ".[dev]"`** is the alternative recommended in **README** / **CURSOR** for developers who want the package on `PYTHONPATH` with extras.

    5. **Optional: lockfiles** — One paragraph: optional **`pip-compile`** / lockfile (reference existing comment in `requirements.txt`); no new files required in this phase.

    6. **Fork checklist** — Bulleted list: clone → `pip install -e ".[dev]"` OR `bash scripts/setup.sh --dev` → `cp .env.example .env` → run tests (`pytest`).
  </action>
  <acceptance_criteria>
    - `test -f docs/DEPENDENCIES.md`
    - `grep -q 'pyproject.toml' docs/DEPENDENCIES.md`
    - `grep -q 'requirements.txt' docs/DEPENDENCIES.md`
    - `grep -q 'pip install -e' docs/DEPENDENCIES.md`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>37-01-02 — Link from README.md</name>
  <read_first>
    - `README.md` (Installation section through “Library-only install”)
  </read_first>
  <action>
    After the **Manual Installation** subsection (or immediately after the **Quick Start** block), add a short subsection **### Dependency files (forks)** with 2–4 sentences and a markdown link: **`[docs/DEPENDENCIES.md](docs/DEPENDENCIES.md)`** explaining that **`pyproject.toml`** is canonical and the doc explains `requirements*.txt` vs editable install.
  </action>
  <acceptance_criteria>
    - `grep -q 'docs/DEPENDENCIES.md' README.md`
    - `grep -q 'Dependency files' README.md`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>37-01-03 — Link from docs/CURSOR.md</name>
  <read_first>
    - `docs/CURSOR.md` (full file)
  </read_first>
  <action>
    After the title block (first paragraph), add one sentence + link: for a full comparison of **`pyproject.toml`** vs **`requirements*.txt`**, see **`docs/DEPENDENCIES.md`**.
  </action>
  <acceptance_criteria>
    - `grep -q 'DEPENDENCIES.md' docs/CURSOR.md`
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks: run `grep -q 'DEPENDENCIES.md' README.md docs/CURSOR.md` and confirm `docs/DEPENDENCIES.md` word count &gt; 200 (substantive).
</verification>
