---
phase: 38-v1-5-backlog-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ROADMAP.md
  - .planning/FUTURE-TODO.md
  - CLAUDE.md
  - .planning/v1.5-CLEANUP-BACKLOG.md
autonomous: true
requirements:
  - TMPL-02
user_setup:
  - None (documentation-only).
must_haves:
  truths:
    - "Root ROADMAP.md Tier 1.3 (yield curve) names the shipped `yc_*` / `add_yield_curve_features` story or clearly maps old names to current columns."
    - "Root ROADMAP.md Tier 1.4 (empirical forward probabilities) names `build_forward_window_probabilities` / `forward_window_probabilities.parquet`, not only legacy `compute_forward_probabilities`."
    - "CLAUDE.md does not list empirical forward probabilities as missing in src while the function exists in `trading_crab_lib.regime`."
    - "FUTURE-TODO.md empirical-forward bullet matches current API + output path."
  artifacts:
    - path: ROADMAP.md
      provides: Tier 1 backlog aligned with transforms/regime
    - path: CLAUDE.md
      provides: Legacy gaps + status consistent with src
    - path: .planning/FUTURE-TODO.md
      provides: Backlog cross-links without stale naming
---

<objective>
Satisfy **TMPL-02**: reconcile **root `ROADMAP.md`**, **`.planning/FUTURE-TODO.md`**, and **`CLAUDE.md`** (legacy gaps / next priority) with **`src/trading_crab_lib`** — **`yc_*`** yield features, **`build_forward_window_probabilities`**, and artifact paths. Remove or retitle stale **“implement `compute_forward_probabilities` only”** wording where the window-probability pipeline already ships.
</objective>

<execution_context>
@.planning/phases/38-v1-5-backlog-reconciliation/38-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/trading_crab_lib/transforms.py
@src/trading_crab_lib/regime.py
@pipelines/04_regime_label.py
@ROADMAP.md
@CLAUDE.md
@.planning/FUTURE-TODO.md
</execution_context>

<tasks>

<task type="auto" tdd="false">
  <name>38-01-01 — Root ROADMAP.md Tier 1.3–1.4</name>
  <read_first>
    - `ROADMAP.md` §1.3–1.4 (full subsections)
    - `transforms.py` — `add_yield_curve_features` (function docstring + column names)
    - `regime.py` — `build_forward_window_probabilities` docstring
    - `pipelines/04_regime_label.py` — parquet path constant / comment header
  </read_first>
  <action>
    **§1.3 Yield curve features:** Replace misleading **`yield_spread_10y2y`** / **`yield_curve_slope`** recipe with the **shipped** design: **`yc_10y_2y`**, **`yc_10y_3m`**, **`yc_2y_3m`** from **`add_yield_curve_features`**, note **fred_gs2** is configured for spreads, and point to **`config/settings.yaml`** `features.*` lists. Keep Tier effort **`S`** if still accurate; adjust “add GS2” text if GS2 is already ingested.

    **§1.4 Empirical forward probabilities:** Rewrite so the **implemented** path is primary: **`build_forward_window_probabilities()`** in **`src/trading_crab_lib/regime.py`**, output **`data/regimes/forward_window_probabilities.parquet`**, horizons from config. One sentence may note legacy **`compute_forward_probabilities`** in **`legacy/regime_analysis.py`** as the historical name. If the feature is considered **done** for Tier 1, either **check off** / **move to Tier 2** “parity extras” or mark **“shipped — see regime.py”** so the roadmap is not a phantom task.

    Preserve markdown style and Tier numbering used elsewhere in **`ROADMAP.md`**.
  </action>
  <acceptance_criteria>
    - `grep -q 'yc_10y_2y\\|add_yield_curve_features' ROADMAP.md`
    - `grep -q 'build_forward_window_probabilities\\|forward_window_probabilities.parquet' ROADMAP.md`
    - `! grep -q 'yield_spread_10y2y' ROADMAP.md` OR grep only in historical context if intentionally retained (prefer removal of stale identifiers)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>38-01-02 — CLAUDE.md gaps and “Next Priority”</name>
  <read_first>
    - `CLAUDE.md` — “Features in legacy NOT yet in src”, “Remaining Gaps”, “Next Priority”, “Known Limitations”
  </read_first>
  <action>
    - **Empirical forward probabilities:** Update **Remaining Gaps** and the bullet under **Features in legacy NOT yet in src** so they **do not** claim the capability is absent from **`src/`**. Options: move to **Closed Gaps** with pointer to **`build_forward_window_probabilities`** + tests, or a single consolidated subsection “Shipped as forward-window table” vs legacy function name.

    - **Yield curve “Next Priority” item 2:** Align with shipped **`yc_*`** (e.g. retitle to “Additional yield-curve / FRED work” if spreads exist; remove duplicate “compute in transforms” if already done).

    - **Confusion matrix / TMPL-03:** Leave as open gap consistent with Phase **39** — do not mark implemented.

    - Refresh test counts in **Current Status** only if materially wrong (optional; avoid drive-by edits).
  </action>
  <acceptance_criteria>
    - `grep -q 'build_forward_window_probabilities' CLAUDE.md`
    - Under **Remaining Gaps**, the **Empirical forward probabilities** subsection no longer states the empirical forward-window table is **not implemented** in **`src/`** (confusion matrix may still be “not implemented”).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>38-01-03 — .planning/FUTURE-TODO.md + v1.5-CLEANUP-BACKLOG.md</name>
  <read_first>
    - `.planning/FUTURE-TODO.md` (product backlog section)
    - `.planning/v1.5-CLEANUP-BACKLOG.md` (if present — forward / yield bullets)
  </read_first>
  <action>
    - **`FUTURE-TODO.md`:** Update the **empirical forward probabilities** bullet to **`build_forward_window_probabilities`**, **`regime.py`**, and **`data/regimes/forward_window_probabilities.parquet`**. Add one-line pointer that **confusion matrix** is tracked under **TMPL-03** / Phase **39** if the file still lists it as undifferentiated backlog.

    - **`v1.5-CLEANUP-BACKLOG.md`:** Append or edit a short **“TMPL-02 (Phase 38)”** note listing which doc files were reconciled and that naming drift items were closed or retitled (do not delete historical context wholesale).
  </action>
  <acceptance_criteria>
    - `grep -q 'build_forward_window_probabilities' .planning/FUTURE-TODO.md`
    - `grep -q 'TMPL-02\\|Phase 38' .planning/v1.5-CLEANUP-BACKLOG.md`
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks: run `rg -n 'compute_forward_probabilities\\(\\)' ROADMAP.md CLAUDE.md .planning/FUTURE-TODO.md` — legacy-only mentions are OK in **legacy** context; product backlog sections should lead with **`build_forward_window_probabilities`** where describing **src/**.
</verification>
