---
phase: 39-v1-5-confusion-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_crab_lib/plotting.py
  - run_pipeline.py
  - pipelines/05_predict.py
  - CLAUDE.md
  - ROADMAP.md
  - .planning/FUTURE-TODO.md
autonomous: true
requirements:
  - TMPL-03
user_setup:
  - None (optional local step 5 run to visually confirm PNG).
must_haves:
  truths:
    - "`plotting.plot_regime_confusion_matrix` (or `plot_confusion_matrix`) saves a PNG under `outputs/plots/` when step 5 runs with plots enabled."
    - "Current-regime RF confusion is derived from `outputs/reports/model_metrics/confusion_matrices.parquet` (or equivalent in-memory path) with `family=regime` and `model=rf`."
    - "CLAUDE.md Remaining Gaps no longer lists confusion matrix visualization as unsupported if the plot ships."
  artifacts:
    - path: src/trading_crab_lib/plotting.py
      provides: Confusion matrix heatmap helper
    - path: run_pipeline.py
      provides: step 5 invokes plot when generate_plots
---

<objective>
Satisfy **TMPL-03**: add a **confusion matrix visualization** for supervised **current-regime** classifiers, implemented in **`plotting.py`**, wired from **step 5** when plots are enabled, saving under **`outputs/plots/`**; update **CLAUDE** / product backlog strings; add or extend **tests** without network.
</objective>

<execution_context>
@.planning/phases/39-v1-5-confusion-matrix/39-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/trading_crab_lib/prediction/model_metrics_artifacts.py
@src/trading_crab_lib/plotting.py
@run_pipeline.py
@pipelines/05_predict.py
@CLAUDE.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>39-01-01 — plotting helper + unit test</name>
  <read_first>
    - `plotting.py` — `_save_or_show`, `PLOT_DIR`, an existing heatmap-style plot if any
    - `model_metrics_artifacts.py` — `_confusion_tidy` and parquet row fields
    - `tests/test_models_reporting.py` — parquet schema smoke patterns
  </read_first>
  <action>
    1. Add **`plot_regime_confusion_matrix`** (or **`plot_confusion_matrix`**) in **`plotting.py`**:
       - Parameters: **`confusion_df: pd.DataFrame, regime_names: dict[int, str] | None, run_cfg: RunConfig`**, optional **`model: str = "rf"`**, **`title`**, **`filename`**.
       - Filter **`confusion_df`**: **`family == "regime"`**, **`model==`**, **`horizon`** is null/NaN (current-regime only).
       - **Pivot** tidy rows to a square matrix (**true_label** × **pred_label**), **sum `count` across folds** (or document if using last fold only — prefer **sum** for stability).
       - Plot **heatmap** (seaborn **`heatmap`** or **`imshow`** with annotations); use **integer** labels on axes; optional **regime_names** for tick labels.
       - Save via **`_save_or_show`** with filename **`step_05_confusion_matrix.png`** (or **`step_05_confusion_matrix_rf.png`**).

    2. Add **`tests/unit/test_plot_confusion_matrix.py`** (or under **`tests/test_plotting.py`** if exists): build a **tiny synthetic** tidy frame matching schema; call plot with **`RunConfig(save_plots=False, show_plots=False)`** or mock **`_save_or_show`** if needed; assert **no exception** and matrix dimensions match unique labels.

    3. Run **`pytest`** on the new test(s) only.
  </action>
  <acceptance_criteria>
    - `pytest` passes for the new test file
    - `grep -q 'def plot_regime_confusion_matrix\\|def plot_confusion_matrix' src/trading_crab_lib/plotting.py` (match actual function name chosen)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>39-01-02 — Wire run_pipeline + optional standalone step 5</name>
  <read_first>
    - `run_pipeline.py` — `step5_predict` plot block after `write_model_metrics_artifacts`
    - `pipelines/05_predict.py` — after metrics write; whether `RunConfig` exists
  </read_first>
  <action>
    1. In **`run_pipeline.py`** **`step5_predict`**, after **`write_model_metrics_artifacts`** and inside **`if run_cfg.generate_plots:`**, **`pd.read_parquet(metrics_dir / "confusion_matrices.parquet")`** if file exists; load **`regime_names`** from YAML as existing block does; call the new plotting function.

    2. **`pipelines/05_predict.py` —** If the script has no **`RunConfig`**, either add a minimal **`--plots`** flag that constructs **`RunConfig(generate_plots=True, save_plots=True)`** and calls the same plot path, **or** document that standalone users use **`run_pipeline.py --steps 5 --plots`**. Pick **one** minimal path (prefer wiring **`run_pipeline`** first; standalone optional).

    3. Log a **warning** on missing parquet / empty filter (do not fail step 5).
  </action>
  <acceptance_criteria>
    - `grep -q 'confusion_matrices.parquet\\|plot_regime_confusion_matrix\\|plot_confusion_matrix' run_pipeline.py`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>39-01-03 — Docs: CLAUDE, ROADMAP, FUTURE-TODO</name>
  <read_first>
    - `CLAUDE.md` — Remaining Gaps, Next Priority, Legacy Alignment
    - `ROADMAP.md` — Tier 1 confusion bullet if present
    - `.planning/FUTURE-TODO.md` — confusion matrix line
  </read_first>
  <action>
    - **`CLAUDE.md`:** Close **Confusion matrix** under **Remaining Gaps** (move to **Closed Gaps** with pointer to **`plotting`** + artifact path) or mark **shipped** with **`step_05_*.png`**.
    - **Next Priority:** Remove or adjust the standalone “confusion matrix plot” item if redundant.
    - **Root `ROADMAP.md`:** Update Tier 1 / suggested session items that still say “add plot_confusion_matrix” as **not started** — mark **done** or **shipped**.
    - **`.planning/FUTURE-TODO.md`:** Mark confusion visualization **done** or point to **`outputs/plots/`** + module.
  </action>
  <acceptance_criteria>
    - `grep -q 'plot_regime_confusion_matrix\\|plot_confusion_matrix\\|confusion matrix' CLAUDE.md`
    - **Remaining Gaps** subsection **Confusion matrix report** no longer asserts visualization is absent from **`plotting.py`** (may reference shipped **`step_05`** PNG).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pytest tests/unit/test_plot_confusion_matrix.py -q` (or path chosen)
- `ruff check src/trading_crab_lib/plotting.py run_pipeline.py` if project uses ruff on changed paths
</verification>
