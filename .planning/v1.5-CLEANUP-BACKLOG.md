# v1.5 — Cleanup & template-hardening backlog

**Status:** **Superseded for execution** by **`.planning/REQUIREMENTS.md`** + **`.planning/ROADMAP.md`** (2026-03-26). Keep this file as **historical notes** from the pre-milestone housekeeping pass.

**Goal:** Keep the repo a **clean fork template**: correct imports, aligned docs, no broken copy-paste examples, and visible product backlog separate from archived GSD phase history.

---

## Repaired (2026-03-26 housekeeping pass)

| Item | Change |
|------|--------|
| Wrong `CheckpointManager` import | `run_pipeline.py` docstring and `RUNBOOK.md` used non-existent `trading_crab_lib.io.checkpoints` → **`trading_crab_lib.checkpoints`**. |
| Root `ROADMAP.md` paths | Replaced legacy `src/market_regime/` with **`src/trading_crab_lib/`**; fixed non-existent **`features/transforms.py`** → **`transforms.py`**, **`regime/profiler.py`** → **`regime.py`**. |
| Notebooks | Replaced **`market_regime`** imports with **`trading_crab_lib`** across `notebooks/*.ipynb` so `pip install -e .` works. |
| `.planning/codebase/` maps | **`STACK.md`**, **`CONCERNS.md`**, **`TESTING.md`**, **`CONVENTIONS.md`**, **`INTEGRATIONS.md`** now reference **`src/trading_crab_lib/`** and **`trading_crab_lib.*`** imports. |
| `scripts/README.md` | Import smoke text updated to **`trading_crab_lib`**. |

**Tests:** `361 passed` (pytest) after the above.

---

## Still outstanding (prioritize in v1.5 or later)

### Documentation drift (historical GSD artifacts)

- **`.planning/phases/*`** — Many archived plans still mention `market_regime` / `src/market_regime/`. That is **historical execution context**; optional follow-up is a scripted replace or a one-line banner in each milestone archive pointing to current package name. **Not blocking** if you treat phase dirs as read-only history.

### Product / code gaps (see also `FUTURE-TODO.md`, root `ROADMAP.md`)

- **Empirical forward-window table:** Documented in root **`ROADMAP.md`** §1.4 and **`CLAUDE.md`** Gap 6 as shipped (**`build_forward_window_probabilities`**, **`forward_window_probabilities.parquet`**). Further work is UX/surfacing, not missing core math.
- **Confusion matrix plot** — ✓ Shipped (**TMPL-03** / Phase **39**): **`plot_regime_confusion_matrix`**, **`outputs/plots/05_confusion_matrix.png`**.
- **Additional FRED series, macrotrends, LightGBM, weekly AI narrative** — Backlog per `ROADMAP.md` Tier 1–2.

### GSD workflow

- **`$gsd-new-milestone`** — **v1.5** requirements are complete (see **`.planning/REQUIREMENTS.md`**); use for **v1.6** or next product focus.
- **`.planning/NEXT_STEPS.md`** — Points to post-v1.5 actions.

### Optional simplifications (non-urgent)

- **`requirements.txt` vs `pyproject.toml`** — Dual dependency sources; consider documenting “single source of truth” for forks (`pyproject.toml` + `pip install -e ".[dev]"`).
- **Notebook outputs** — Cleared or re-run notebooks if you want logs to show `trading_crab_lib` in saved outputs only (cosmetic).

---

## TMPL-02 (Phase 38) — backlog doc reconciliation *(2026-03-27)*

Reconciled **root `ROADMAP.md`** §1.3–1.4, **`CLAUDE.md`** (legacy gaps / next priority), **`.planning/FUTURE-TODO.md`**, and this file where referenced, with **`src/trading_crab_lib`**: **`yc_*`** / **`add_yield_curve_features`**, **`build_forward_window_probabilities`** / **`forward_window_probabilities.parquet`**. Stale “implement only legacy **`compute_forward_probabilities`**” / **`yield_spread_*`** placeholder names in product-facing backlog were closed or retitled.

---

## Suggested v1.5 requirements themes (for REQUIREMENTS.md)

1. **Doc parity sweep** — Root docs + `.planning/codebase/*` + one pass on high-traffic phase READMEs.
2. **Notebook + fork guide** — `notebooks/README.md` (added) + link from main `README.md` if desired.
3. **Backlog triage** — Close or re-scope `ROADMAP.md` items that are already implemented (forward-window probabilities, yield-curve columns if present in `transforms.py`).
